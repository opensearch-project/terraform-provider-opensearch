---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "opensearch_correlation_rule Data Source - terraform-provider-opensearch"
subcategory: ""
description: |-
  Use this data source to retrieve information about an existing OpenSearch correlation rule.
---

# opensearch_correlation_rule (Data Source)

Use this data source to retrieve information about an existing OpenSearch correlation rule.

## Example Usage

```terraform
# Retrieve an existing correlation rule by ID
data "opensearch_correlation_rule" "existing" {
  rule_id = "MLm_zZ0BghYda8SUa-hc"
}

# Use the correlation rule data in outputs
output "correlation_rule_name" {
  value = data.opensearch_correlation_rule.existing.name
}

output "correlation_queries" {
  value = data.opensearch_correlation_rule.existing.correlate
}

output "time_window_minutes" {
  value = data.opensearch_correlation_rule.existing.time_window / 60000
}

# Reference data from a created resource
resource "opensearch_correlation_rule" "new" {
  name        = "New Correlation Rule"
  time_window = 300000

  correlate {
    index    = "vpc_flow"
    query    = "suspicious_traffic:true"
    category = "network"
  }

  correlate {
    index    = "app_logs"
    query    = "error:true"
    category = "others_application"
  }
}

data "opensearch_correlation_rule" "created" {
  rule_id = opensearch_correlation_rule.new.id
}

# Use in another resource configuration
resource "opensearch_monitor" "correlation_monitor" {
  body = jsonencode({
    name = "Monitor for ${data.opensearch_correlation_rule.created.name}"
    type = "monitor"
    # ... other monitor configuration
  })
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `rule_id` (String) The ID of the correlation rule to retrieve.

### Read-Only

- `correlate` (List of Object) List of correlation queries. (see [below for nested schema](#nestedatt--correlate))
- `id` (String) The ID of this resource.
- `name` (String) Name of the correlation rule.
- `time_window` (Number) Time window in milliseconds within which correlations must occur.
- `trigger` (List of Object) Alert trigger configuration for the correlation rule. (see [below for nested schema](#nestedatt--trigger))

<a id="nestedatt--correlate"></a>
### Nested Schema for `correlate`

Read-Only:

- `category` (String)
- `field` (String)
- `index` (String)
- `query` (String)


<a id="nestedatt--trigger"></a>
### Nested Schema for `trigger`

Read-Only:

- `actions` (List of Object) (see [below for nested schema](#nestedobjatt--trigger--actions))
- `name` (String)
- `severity` (String)

<a id="nestedobjatt--trigger--actions"></a>
### Nested Schema for `trigger.actions`

Read-Only:

- `destination_id` (String)
- `message_template` (List of Object) (see [below for nested schema](#nestedobjatt--trigger--actions--message_template))
- `name` (String)
- `subject_template` (List of Object) (see [below for nested schema](#nestedobjatt--trigger--actions--subject_template))
- `throttle` (List of Object) (see [below for nested schema](#nestedobjatt--trigger--actions--throttle))
- `throttle_enabled` (Boolean)

<a id="nestedobjatt--trigger--actions--message_template"></a>
### Nested Schema for `trigger.actions.message_template`

Read-Only:

- `lang` (String)
- `source` (String)


<a id="nestedobjatt--trigger--actions--subject_template"></a>
### Nested Schema for `trigger.actions.subject_template`

Read-Only:

- `lang` (String)
- `source` (String)


<a id="nestedobjatt--trigger--actions--throttle"></a>
### Nested Schema for `trigger.actions.throttle`

Read-Only:

- `unit` (String)
- `value` (Number)
